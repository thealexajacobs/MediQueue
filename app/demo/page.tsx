import { DemoExperience } from '@/components/demo/DemoExperience';

export const metadata = {
  title: 'MediQueue Demo — See Queue Management in Action',
  description: 'Interactive demo of MediQueue clinic queue management system. Explore the dashboard, patient tracking, and analytics.',
};

export default function DemoPage() {
  return <DemoExperience />;
}
