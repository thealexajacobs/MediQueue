# Database
DATABASE_URL=

# Auth.js v5
AUTH_SECRET=
AUTH_URL=http://localhost:3000

# Socket.IO (separate server)
SOCKET_SERVER_URL=
SOCKET_AUTH_TOKEN=                    # Shared secret for Socket.IO emit auth

# Upstash Redis (rate limiting)
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

# Set to "true" to skip env validation at startup (not recommended)
# SKIP_ENV_VALIDATION=
